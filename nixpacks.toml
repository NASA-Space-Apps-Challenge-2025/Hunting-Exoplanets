[start]
cmd = "python3 -m uvicorn backend.app:app --host 0.0.0.0 --port $PORT"

[phases.setup]
nixPkgs = ["python311", "gcc", "gfortran"]

[phases.install]
cmds = [
  "pip3 install --upgrade pip",
  "pip3 install -r requirements.txt"
]
